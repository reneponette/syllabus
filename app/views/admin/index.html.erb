<div class="panel panel-default">
	<div class="panel-heading">
		<% if @_admin %>
		관리자로 로그인 하였습니다.
		<a href="/admin/logout"><button class="btn btn-default">Sign out</button></a>
		<% else %>
		<%= render :partial => 'form' %>
		<% end %>
	</div>
	
	<div class="panel-body">
	</div>
</div>
